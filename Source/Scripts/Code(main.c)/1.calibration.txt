int main(void)
{
    HAL_Init();
    __HAL_RCC_GPIOD_CLK_ENABLE();
    pca9685_init();

    // 暂时去掉循环里的 Delay，直接连续发送指令
    for(int i = 0; i < 64; i++) {
        set_servo_angle(i, 90); 
    }

    while(1)
    {
        // 维持程序运行，不使用 HAL_Delay
    }
}